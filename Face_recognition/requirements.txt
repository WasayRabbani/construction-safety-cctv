# --- STANDARD DEPENDENCIES ---
opencv-python
tqdm
flask
flask-cors
ultralytics
pandas
imageio-ffmpeg
setuptools<70.0.0
numpy<2
cloudinary

# --- FAST FACIAL RECOGNITION ---
# Local: dlib + face_recognition is installed manually in .venv (not in this file)
# Cloud: facenet-pytorch uses PyTorch (already installed) — no C++ compilation!
facenet-pytorch
pillow

# --- GPU ACCELERATION (OPTIONAL) ---
# If you have an NVIDIA GPU, run these commands manually:
#
# 1. For YOLO GPU Support:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
